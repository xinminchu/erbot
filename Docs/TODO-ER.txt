Nov 7, 2025

 Work in Progress
 • Established the parameter tuning framework (`70_tuning.R`) and integrated it with the main ER
 pipeline (`er_main()`, `run_cora()`).
 • Defined major parameter groups (clustering, similarity, thresholds, iterations) and generated the
 reference summary PDF.
 • Implemented the initial tuning function structure (e.g., `er_tune()`), supporting grid or random
 search using `expand.grid()` and `purrr::map()`.
 • Integrated internal evaluation metrics (ARI, silhouette, modularity) for automatic best-parameter
 selection.
 • Added roxygen2 documentation and testthat structure for initial testing.
 • Generated reference summary tables for Edit-distance, SN, CPM, Louvain, and other methods.
 • Implement adaptive or hierarchical search methods (e.g., Bayesian optimization) to improve
 efficiency.
 • Define unified performance criteria (e.g., ARI or silhouette) and add early-stopping mechanism.
 • Standardize output structure for tuning results and create visualization tools (e.g.,
 `er_plot_tuning_curve()`).
 • Integrate tuning process into the main pipeline (`er_main(..., tune=TRUE)`) for full automation.
 • Run tuning experiments on CORA, Affiliation, and NCVoter datasets; compare tuned vs. default
 parameters.
 • Record runtime performance and analyze algorithmic efficiency.


Oct 24, 2025

Read papers on parameter tuning, ER benchmarks,

Reorganize and improve R package 'erbot'.

No results to report yet, no specific question.

What is the source paper for SN? --
Hernández, M. A. & Stolfo, S. J. (1995). “The Merge/Purge Problem for Large Databases.” SIGMOD ’95.
for Edit distance --
Levenshtein, V. I. (1966). “Binary codes capable of correcting deletions, insertions, and reversals.” Soviet Physics Doklady 10 (8): 707–710.

This algorithm in ERBOT unites:

Sorted-Neighborhood (SN) candidate generation, and
Edit-distance thresholding for linkage,
optionally MST-based pruning for robust graph clustering.

Todo:
Implement parameter tuning in R package 'erbot'.

Keep tractable with recording methods source papers.



Oct 17, 2025
Refs:
Neuhof, F., Fisichella, M., Papadakis, G., Nikoletos, K., Augsten, N., Nejdl, W., & Koubarakis, M. (2024). Open benchmark for filtering techniques in entity resolution. VLDB J., 33, 1671-1696. https://doi.org/10.1007/s00778-024-00868-7.

Hassanzadeh, O., Chiang, F., Miller, R., & Lee, H. (2009). Framework for Evaluating Clustering Algorithms in Duplicate Detection. Proc. VLDB Endow., 2, 1282-1293. https://doi.org/10.14778/1687627.1687771.

Benchmarks:
Continuous Benchmark of ER Filtering Workflows at https://github.com/gpapadis/ContinuousFilteringBenchmark

Todo:
Figure out the best tools based on performance of ER methods comparison;

How to calculate weights for multi fields smartly?
check Mhalanobis distance (The Mahalanobis distance is a measure of the distance between a point
P{\displaystyle P} and a probability distribution D{\displaystyle D}, introduced by P. C. Mahalanobis in 1936. From Wikipedia)



Oct 10, 2025

1) Get whole picture of mainstream ER methods through Concensus.app (see shared link by Prof Degras: https://consensus.app/search/what-are-the-main-methods-for-entity-resolution-an/VEjsGVyFTdycYODpmWEnnQ/?utm_source=share&utm_medium=clipboard)
2) Done vs To-be-done checking (Fellegi-Sunter not efficient, require blocking; deep learning for unsupervised)
3) Benchmark work: find better methods. e.g. two-step flow (binary classification then clustering) ---- cref. MOABB.


-----------------------------------------------
Oct 3, 2025

1) tuning parameter selection
2) make sure that all mainstream ER methods are included in your benchmark
3) computing time/space and scalability of methods

