% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-reporting.R
\name{er_data_profile}
\alias{er_data_profile}
\title{Dataset profile PDF report}
\usage{
er_data_profile(
  data,
  diag = NULL,
  output_file = NULL,
  title = NULL,
  top_k_cats = 10L,
  hist_bins = 20L,
  max_fields = 30L
)
}
\arguments{
\item{data}{A \code{data.frame} or the raw result from \code{\link[=er_load_input]{er_load_input()}}.}

\item{diag}{Optional. The list returned by \code{er_diagnose()}.  When \code{NULL}
the function still runs but skips diagnosis-derived panels.}

\item{output_file}{Character.  Path for the output PDF.  Defaults to
\code{"data_profile_<timestamp>.pdf"} in the current working directory.}

\item{title}{Character.  Report title shown on the cover page.  Defaults to
the output file base name.}

\item{top_k_cats}{Integer.  Maximum number of categories shown in
categorical bar charts (default \code{10}).}

\item{hist_bins}{Integer.  Number of histogram bins for numeric / word-count
charts (default \code{20}).}

\item{max_fields}{Integer.  Maximum number of field pages to include
(default \code{30}).  Fields are taken in column order.}
}
\value{
(Invisible) the output file path.
}
\description{
Reads loaded data (from \code{\link[=er_load_input]{er_load_input()}} or a plain \code{data.frame}) and an
optional diagnosis (from \code{er_diagnose()}) and writes a multi-page PDF
summarising the dataset's structure and field characteristics.
}
\details{
Pages produced:
\enumerate{
\item Cover — title, overview statistics, and a field-type bar chart.
\item One page per field — info panel, value-distribution chart, and a
missingness bar.  Limited to \code{max_fields} fields.
\item Recommendations — blocking strategy and per-field similarity methods.
}
}
\examples{
\dontrun{
## Basic usage — no ground truth needed
df   <- er_load_input("my_data.csv")
diag <- er_diagnose(df)
er_data_profile(df, diag, output_file = "reports/my_data_profile.pdf",
                title = "My Dataset — Profile")

## Skip diagnosis (quick overview only)
er_data_profile(df, output_file = "quick_profile.pdf")
}
}
