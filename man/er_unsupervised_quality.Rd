% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09-evaluate.R
\name{er_unsupervised_quality}
\alias{er_unsupervised_quality}
\title{Unsupervised clustering quality scores}
\usage{
er_unsupervised_quality(pred, S, max_pairs_for_sil = 5000L)
}
\arguments{
\item{pred}{Integer vector of cluster labels.}

\item{S}{Symmetric \code{dgCMatrix} (n × n) of combined similarities.}

\item{max_pairs_for_sil}{Integer. Max pairs sampled for silhouette
(expensive for large n). Default \code{5000L}.}
}
\value{
Named list: \code{within_sim}, \code{between_sim}, \code{sim_gap},
\code{silhouette}.
}
\description{
When no ground truth is available, computes:
\enumerate{
\item Within-cluster similarity (mean S(i,j) for same-cluster pairs).
\item Between-cluster similarity (mean S(i,j) for different-cluster pairs).
\item Similarity gap (within - between; larger is better).
\item Average silhouette width on D = 1 - S.
}
}
