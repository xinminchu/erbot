% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11-ablation.R
\name{er_ablation_all}
\alias{er_ablation_all}
\title{Full ablation study: single-run conditions + CV + baselines}
\usage{
er_ablation_all(
  data,
  truth = NULL,
  id_col = NULL,
  block_spec = "auto",
  cluster_methods = c("hclust_avg", "hclust_ward", "gc", "leiden", "louvain",
    "label_prop", "threshold_cc"),
  K = 5L,
  seed = 42L,
  run_cv = TRUE,
  run_baselines = TRUE,
  baselines = c("fs_em", "splink", "deepmatcher", "ditto"),
  cv_out_dir = NULL,
  out_dir = NULL,
  conditions = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{data.frame of records.}

\item{truth}{Optional ground truth.}

\item{id_col}{Character. ID column.}

\item{block_spec}{Blocking spec for the blocking conditions.}

\item{cluster_methods}{Character vector.}

\item{K}{Integer. Number of CV folds (default 5).}

\item{seed}{Integer. CV fold seed (default 42).}

\item{run_cv}{Logical. Whether to run the (expensive) CV conditions.}

\item{run_baselines}{Logical. Whether to run external Python baselines.}

\item{baselines}{Character vector. Which baselines to run.}

\item{cv_out_dir}{Character. Optional directory for CV fold CSV output.}

\item{out_dir}{Character. Optional directory for full ablation CSV output.}

\item{verbose}{Logical.}
}
\value{
A named list:
\item{\code{ablation_table}}{Single-run condition results.}
\item{\code{cv_block}}{CV results with blocking.}
\item{\code{cv_no_block}}{CV results without blocking.}
\item{\code{baselines}}{External baseline results.}
\item{\code{combined}}{Single flat data.frame merging all above.}
}
\description{
Combines:
\enumerate{
\item \strong{Ablation table} (6 single-run conditions from
\code{\link{er_ablation_table}}).
\item \strong{CV conditions}: honest 5-fold CV with
blocking+cv_ari and no_block+cv_ari (via \code{\link{er_cv}}).
\item \strong{External baselines}: Fellegi–Sunter EM, Splink,
DeepMatcher, Ditto (via \code{\link{er_run_baselines}}).
}
}
\details{
Any component that fails (Python not installed, timeout, etc.) is
represented as NAs in the output table with a warning.
}
