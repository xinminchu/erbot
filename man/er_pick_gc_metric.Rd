% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-reporting.R
\name{er_pick_gc_metric}
\alias{er_pick_gc_metric}
\title{Pick GC metric name from tuning table}
\usage{
er_pick_gc_metric(gc_curve, fallback_metric = "adj_rand")
}
\arguments{
\item{gc_curve}{A \code{data.frame}/\code{tibble} of tuning results with one column
per metric, as returned by \code{er_gc_from_text()}.}

\item{fallback_metric}{Character. Metric name to try if \code{"silhouette"} is
absent. Default \code{"adj_rand"}.}
}
\value{
A single character metric name, or \code{NULL} if none found.
}
\description{
Returns the name of the best metric column to use from a graph-coloring
threshold-sweep table. Prefers \code{"silhouette"} when present (unsupervised),
otherwise tries \code{fallback_metric} and then a fixed priority list.
}
