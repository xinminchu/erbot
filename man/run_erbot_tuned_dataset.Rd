% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/40-run-with-dataset.R
\name{run_erbot_tuned_dataset}
\alias{run_erbot_tuned_dataset}
\title{Tune ERBOT parameters for a chosen benchmark dataset}
\usage{
run_erbot_tuned_dataset(
  dataset = c("cora", "affiliation", "d10k"),
  out_dir,
  data_affil = NULL,
  truth_affil = NULL,
  data_d10k = NULL,
  truth_d10k = NULL,
  digits = 5,
  top_n = 5,
  objective = "silhouette_penalized"
)
}
\arguments{
\item{dataset}{Character scalar, one of \code{"cora"}, \code{"affiliation"},
or \code{"d10k"}. Determines which dataset loader, field set, methods,
and tuning grids are used.}

\item{out_dir}{Character scalar; path to a directory where all outputs
will be written. Subdirectories \verb{data/} and \verb{results/} are created
if necessary. Tuning PDF files are saved inside \verb{results/}.}

\item{data_affil}{Character scalar or \code{NULL}. Path to the affiliation
data CSV (e.g., \code{"D:/erbot/data/affiliationstrings_ids.csv"}).
Must be provided when \code{dataset = "affiliation"}.}

\item{truth_affil}{Optional ground-truth specification for the
affiliation dataset: a path to a mapping file or any object accepted
by \code{\link[=er_truth_from_any]{er_truth_from_any()}}. May be \code{NULL} if external evaluation is
not required.}

\item{data_d10k}{Character scalar or \code{NULL}. Path to the D10K synthetic
dataset CSV (e.g., \code{"D:/erbot/data/D10K.csv"}). Must be provided when
\code{dataset = "d10k"}.}

\item{truth_d10k}{Optional ground-truth specification for D10K: a path
or object accepted by \code{\link[=er_truth_from_any]{er_truth_from_any()}}. May be \code{NULL} to tune
purely on internal criteria.}

\item{digits}{Integer; number of digits used when summarizing numeric
results in the tuning PDF report.}

\item{top_n}{Integer; number of top clusters, parameter settings, or
items to display in the tuning report (depending on the structure
supported by \code{\link[=er_save_report_pdf]{er_save_report_pdf()}}).}

\item{objective}{Character scalar giving the tuning objective passed
to \code{\link[=er_tune]{er_tune()}}. Typical choices include \code{"silhouette_penalized"} or
other internal or external criteria implemented in ERBOT.}
}
\value{
Invisibly returns the tuned ER object returned by \code{\link[=er_tune]{er_tune()}},
which typically contains:
\itemize{
\item the best method and parameter configuration(s),
\item tuning scores per configuration,
\item and (optionally) external evaluation metrics when truth is provided.
}
}
\description{
High-level wrapper around \code{\link[=er_tune]{er_tune()}} that runs parameter tuning for
one of the standard benchmark datasets (\code{"cora"}, \code{"affiliation"},
or \code{"d10k"}), using dataset-specific grids and tuning settings. For
each dataset, this function:
}
\details{
\itemize{
\item Loads the data (and optional truth) via \code{\link[=er_load_input]{er_load_input()}} or
dataset-specific helpers.
\item Constructs reasonable method-specific tuning grids for ER methods
such as \code{"kmeans"}, \code{"louvain"}, \code{"leiden"}, \code{"mst_edit"},
\code{"threshold_cc"}, etc.
\item Calls \code{\link[=er_tune]{er_tune()}} with an internal or external objective
(e.g. \code{"silhouette_penalized"}).
\item Writes a tuning-aware PDF report via \code{\link[=er_save_report_pdf]{er_save_report_pdf()}} into the
\verb{results/} subfolder of \code{out_dir}.
}

This is intended as a reproducible, dataset-aware tuning entry point
for the ERBOT benchmark suite.

For each dataset, the function constructs dataset-appropriate grids:
\itemize{
\item \strong{CORA}: richer grids for \code{"kmeans"}, \code{"louvain"}, \code{"leiden"}, and
\code{"mst_edit"} over a moderate search space, with higher stability
and sample sizes.
\item \strong{Affiliation}: emphasizes text-based MST and graph methods on a
single affiliation string field, using coarser grids to keep runtime
reasonable.
\item \strong{D10K}: uses lighter grids and smaller SVD dimension to keep tuning
feasible on a large synthetic dataset, with fewer bootstrap replicates.
}
}
\examples{
\dontrun{
# Tune methods on CORA with silhouette-penalized objective
tuned_cora <- run_erbot_tuned_dataset(
  dataset  = "cora",
  out_dir  = "outputs/cora_tuned",
  objective = "silhouette_penalized"
)

# Tune methods on Affiliation with CSV input and mapping file
tuned_affil <- run_erbot_tuned_dataset(
  dataset     = "affiliation",
  out_dir     = "outputs/affil_tuned",
  data_affil  = "data/affiliationstrings_ids.csv",
  truth_affil = "data/affiliationstrings_mapping.csv"
)
}

}
