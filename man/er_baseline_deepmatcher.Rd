% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-baselines.R
\name{er_baseline_deepmatcher}
\alias{er_baseline_deepmatcher}
\title{DeepMatcher deep learning baseline}
\usage{
er_baseline_deepmatcher(
  data,
  truth,
  id_col = NULL,
  block = "auto",
  model = "hybrid",
  epochs = 10L,
  threshold = 0.5,
  tmp_dir = tempdir(),
  verbose = TRUE
)
}
\arguments{
\item{data}{data.frame of records.}

\item{truth}{Ground truth (required for DeepMatcher training).}

\item{id_col}{Character. ID column.}

\item{block}{Blocking spec for candidate generation.}

\item{model}{Character. DeepMatcher model type: \code{"hybrid"},
\code{"rnn"}, \code{"attention"}, \code{"sif"} (default \code{"hybrid"}).}

\item{epochs}{Integer. Training epochs (default 10).}

\item{threshold}{Numeric. Match probability threshold (default 0.5).}

\item{tmp_dir}{Character. Working directory for data exchange.}

\item{verbose}{Logical.}
}
\value{
Named list: \code{performance}, \code{predictions}, \code{raw}.
}
\description{
Calls the Python \pkg{deepmatcher} library via \code{system2()}.
DeepMatcher trains a recurrent/attention network on labelled record pairs
and predicts match/non-match for all candidate pairs.
}
\details{
Requires a labelled train set (from \code{truth}).  A 70/30 internal
split of gold pairs + sampled non-matches is used for train/validation.
}
