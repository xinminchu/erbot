% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/40-run-with-dataset.R
\name{run_erbot_dataset}
\alias{run_erbot_dataset}
\title{Run ERBOT demo pipeline for a chosen benchmark dataset}
\usage{
run_erbot_dataset(
  dataset = c("cora", "affiliation", "d10k"),
  out_dir,
  fields_cora = c("title", "authors", "address"),
  data_affil = NULL,
  truth_affil = NULL,
  fields_affil = "affil1",
  data_d10k = NULL,
  truth_d10k = NULL,
  fields_d10k = c("name", "address", "city", "state", "zip"),
  digits = 5,
  top_n = 5,
  k_grid_common = seq(10, 500, by = 10)
)
}
\arguments{
\item{dataset}{Character scalar, one of \code{"cora"}, \code{"affiliation"},
or \code{"d10k"}. Controls which dataset-specific branch is used.}

\item{out_dir}{Character scalar; path to a directory where all outputs
will be written. Subdirectories \verb{data/} and \verb{results/} are created
if they do not already exist.}

\item{fields_cora}{Character vector of field names to use for the CORA
dataset (default is \code{c("title", "authors", "address")}). Passed to
\code{\link[=run_cora]{run_cora()}}.}

\item{data_affil}{Character scalar or \code{NULL}. Path to the affiliation
data CSV (e.g., \code{"D:/erbot/data/affiliationstrings_ids.csv"}). Must
be provided when \code{dataset = "affiliation"}.}

\item{truth_affil}{Character scalar or \code{NULL}. Optional path to a
mapping file with true clusters for the affiliation data (e.g.,
\code{"D:/erbot/data/affiliationstrings_mapping.csv"}). If \code{NULL},
the affiliation run proceeds without external evaluation.}

\item{fields_affil}{Character vector of fields to use for the
affiliation dataset (default is \code{"affil1"}).}

\item{data_d10k}{Character scalar or data.frame. For the D10K synthetic
dataset, either a CSV path (e.g., \code{"D:/erbot/data/D10K.csv"}) or a
preloaded data.frame of records. Must be provided when
\code{dataset = "d10k"}.}

\item{truth_d10k}{Optional ground-truth specification for D10K. Can be
a path to a file, an id+cluster table, or a pair list. It is passed
through to \code{\link[=er_truth_from_any]{er_truth_from_any()}} inside \code{\link[=er_main]{er_main()}}. May be \code{NULL}
if external evaluation is not required.}

\item{fields_d10k}{Character vector of field names to use for the D10K
dataset (default is \code{c("name", "address", "city", "state", "zip")}).}

\item{digits}{Integer; number of digits used when writing performance
tables to CSV and in PDF reports.}

\item{top_n}{Integer; number of top clusters or records to display in
the PDF report (if supported by \code{\link[=er_save_report_pdf]{er_save_report_pdf()}}).}

\item{k_grid_common}{Numeric vector giving the common grid of
candidate \code{k} values (e.g., for K-means) used in the D10K branch.}
}
\value{
Invisibly returns the ER result object produced by the
underlying dataset-specific runner:
\itemize{
\item For \code{"cora"}: the result from \code{\link[=run_cora]{run_cora()}}.
\item For \code{"affiliation"}: the result from \code{\link[=run_affiliation]{run_affiliation()}}.
\item For \code{"d10k"}: the result from \code{\link[=er_main]{er_main()}}.
}
}
\description{
High-level wrapper that runs ERBOT on one of the standard benchmark
datasets (\code{"cora"}, \code{"affiliation"}, or \code{"d10k"}), with dataset-specific
loaders, fields, and output file names. This function is intended as a
reproducible demo runner for generating performance tables and PDF
reports for each dataset.
}
\details{
For:
\itemize{
\item \code{"cora"}: uses the \pkg{cora} package and the internal \code{cora_gold}
labels via \code{\link[=run_cora]{run_cora()}}.
\item \code{"affiliation"}: reads affiliation strings from a CSV and optionally
uses a mapping file of true clusters via \code{\link[=run_affiliation]{run_affiliation()}}.
using ground-truth labels in any format supported by \code{\link[=er_truth_from_any]{er_truth_from_any()}}.
}

All variants create a simple output folder layout with \verb{data/} for
predicted clusters and \verb{results/} for performance tables and PDF reports.
}
\examples{
\dontrun{
# Run the CORA demo and write outputs into "outputs/cora"
run_erbot_dataset(
  dataset = "cora",
  out_dir = "outputs/cora"
)

# Run the Affiliation demo with CSV paths
run_erbot_dataset(
  dataset     = "affiliation",
  out_dir     = "outputs/affil",
  data_affil  = "data/affiliationstrings_ids.csv",
  truth_affil = "data/affiliationstrings_mapping.csv"
)
}

}
