% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07-cluster.R
\name{er_cluster_all}
\alias{er_cluster_all}
\title{Run all clustering methods}
\usage{
er_cluster_all(
  S,
  methods = "all",
  k = NULL,
  k_grid = c(5L, 10L, 15L, 20L, 30L, 50L),
  threshold = 0.5,
  gc_thresholds = NULL,
  resolution = 1,
  leiden_resolutions = c(0.05, 0.1, 0.2, 0.5, 1),
  X = NULL,
  svd_dim = 50L,
  truth_vec = NULL,
  tune_metric = "adj_rand",
  verbose = TRUE
)
}
\arguments{
\item{S}{Symmetric \code{dgCMatrix} (n × n).}

\item{methods}{Character vector of methods, or \code{"all"} for all
supported methods. Unavailable optional methods (leiden, gc) are silently
skipped.}

\item{k}{Integer. Cluster count for centroidal methods (tuned if \code{NULL}).}

\item{k_grid}{Integer vector for k auto-tuning sweep.}

\item{threshold}{Numeric. Similarity threshold for \code{"threshold_cc"} and
\code{"gc"}.}

\item{gc_thresholds}{Numeric vector. If provided, overrides \code{threshold}
for graph coloring and sweeps, selecting best by silhouette or ARI.}

\item{resolution}{Numeric. Louvain/Leiden resolution.}

\item{X}{Optional feature matrix for centroidal methods.}

\item{svd_dim}{Integer. SVD dimensions when deriving X from S.}

\item{truth_vec}{Optional integer vector of ground-truth labels for supervised
k-tuning.}

\item{tune_metric}{Character. Metric for supervised tuning.
Default \code{"adj_rand"}.}

\item{verbose}{Logical. Print progress.}
}
\value{
Named list of integer vectors (one per method).
}
\description{
Applies every requested method to the combined similarity matrix \code{S}
and returns a named list of label vectors.
}
