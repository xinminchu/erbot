% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-mst-sn-edit.R
\name{er_mst_or_sn_edit}
\alias{er_mst_or_sn_edit}
\title{MST or Sorted-Neighborhood (SN) + edit-distance clustering}
\usage{
er_mst_or_sn_edit(
  text_vec,
  mst_cut_ratio = 5,
  mst_k = NULL,
  sn_window = 40,
  sn_method = "jw",
  sn_thresh = 0.12
)
}
\arguments{
\item{text_vec}{Character vector of strings to cluster.}

\item{mst_cut_ratio}{Non-negative numeric controlling how aggressively to cut
long MST edges; larger values keep more edges. Default \code{5}.}

\item{mst_k}{Optional integer. If provided, also build a k-NN graph in the
sorted order (each node connects to the next \code{mst_k} items) with
weights equal to edit distance, then compute its MST and prune with the
robust rule above.}

\item{sn_window}{Integer sliding-window size (>= 1) for sorted-neighborhood
candidate pairs. Default \code{40}.}

\item{sn_method}{String distance method for \code{stringdist::stringdist};
e.g., \code{"jw"} (Jaro–Winkler), \code{"lv"} (Levenshtein). Default \code{"jw"}.}

\item{sn_thresh}{Numeric in \verb{[0, 1]} for methods returning distances in that
range (e.g., Jaro–Winkler). Create an SN edge if distance <= \code{sn_thresh}.
Default \code{0.12}.}
}
\value{
Integer vector of cluster labels (length = \code{length(text_vec)}).
}
\description{
Builds links between near-duplicate strings using a Sorted-Neighborhood
pass with an edit-distance threshold. Optionally, also builds a
k-nearest-neighbor graph in the sorted order and prunes a Minimum
Spanning Tree (MST) using a robust cut rule.
}
\details{
The final graph is the union of:
\itemize{
\item SN edges where \code{stringdist(text[i], text[j], method = sn_method) <= sn_thresh}
for pairs within a sliding window of size \code{sn_window} on the
lexicographically sorted normalized text, and
\item (optional) MST edges whose distances are \emph{not} outliers:
\code{d <= median(d) + mst_cut_ratio * MAD(d)}.
}

Connected components of this union graph define clusters.
}
