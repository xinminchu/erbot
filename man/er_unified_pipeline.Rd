% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-pipeline.R
\name{er_unified_pipeline}
\alias{er_unified_pipeline}
\title{Unified ER pipeline (full)}
\usage{
er_unified_pipeline(
  data,
  truth = NULL,
  sheet = NULL,
  id_col = NULL,
  fields = NULL,
  extra_fields = NULL,
  id_candidates = c("id", "affiliation_id", "record_id", "rec_id", "docid", "rowid",
    "paper_id"),
  text_candidates = c("title", "name", "raw_title", "string", "text", "affiliation",
    "aggregate value", "clean ag.value"),
  embed_col = NULL,
  embed_candidates = c("embedded clean ag.value", "embedded ag.value", "emb",
    "embedding", "vector", "embedding_clean"),
  k_clusters = 10,
  knn_k = 15,
  mst_cut_ratio = 5,
  mst_k = NULL,
  sn_window = 40,
  sn_method = "jw",
  sn_thresh = 0.12,
  svd_dim = 100,
  louvain_min_sim = 0,
  cos_thresh = 0.88,
  gc_thresholds = seq(0.06, 0.2, 0.02),
  gc_method = c("lf", "sl", "rlf"),
  gc_dist_method = "jw",
  auto_tune = TRUE,
  tune_metric = "adj_rand",
  k_grid = c(5, 10, 15, 20),
  eval_mode = c("labeled_only", "singleton_fill"),
  write_csv = NULL,
  run_comm_methods = c("walktrap", "infomap", "fast_greedy", "label_prop"),
  show_progress = TRUE
)
}
\arguments{
\item{truth}{optional truth (see \link{er_truth_from_any})}

\item{id_col}{optional id name}

\item{fields}{vector of fields to concatenate}

\item{embed_col}{name of embedding column if present}

\item{k_clusters}{default k for centroidal methods}

\item{knn_k}{k for kNN graphs}

\item{gc_thresholds}{thresholds to sweep for GC}

\item{auto_tune}{logical: tune k by silhouette (or CA when truth)}
}
\value{
list with performance, predictions, tuning, details
}
\description{
Unified ER pipeline (full)
}
