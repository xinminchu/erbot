% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-baselines.R
\name{er_baseline_fs_em}
\alias{er_baseline_fs_em}
\title{Fellegi–Sunter EM baseline}
\usage{
er_baseline_fs_em(
  data,
  truth = NULL,
  id_col = NULL,
  block = "auto",
  n_levels = 3L,
  n_iter = 20L,
  pi_m_init = 0.1,
  log_odds_threshold = 0,
  verbose = TRUE
)
}
\arguments{
\item{data}{data.frame of records.}

\item{truth}{Optional ground truth (named int vector or tibble(id, cluster_id)).}

\item{id_col}{Character. ID column. Auto-detected if NULL.}

\item{block}{Blocking spec passed to \code{er_block()}.}

\item{n_levels}{Integer. Number of discretisation bins per field (default 3).}

\item{n_iter}{Integer. EM iterations (default 20).}

\item{pi_m_init}{Numeric. Initial match prior (default 0.1).}

\item{log_odds_threshold}{Numeric. Classification threshold on log-odds.}

\item{verbose}{Logical.}
}
\value{
A named list: \code{performance} (data.frame), \code{predictions}
(tibble), \code{weights} (named vector), \code{log_odds} (pair-level).
}
\description{
Implements a proper joint Fellegi–Sunter comparison-vector EM
(Fellegi & Sunter 1969; Larsen & Rubin 2001).  Unlike the per-field
approximation in \code{er_weights(method="fellegi_sunter")}, this version:
\enumerate{
\item Discretises each field similarity into \eqn{L} levels (default 3).
\item Runs EM on the joint comparison vector under conditional-independence
mixture: \eqn{P(\boldsymbol{\gamma} | M)} and
\eqn{P(\boldsymbol{\gamma} | U)}, each a product of per-field
multinomials.
\item Classifies pairs above a log-odds threshold as matches.
\item Applies threshold-connected-components to build entity clusters.
}
}
\details{
No ground truth is required; this is a fully unsupervised baseline.
}
