% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11-ablation.R
\name{er_ablation_table}
\alias{er_ablation_table}
\title{Run ablation conditions (single-run, not CV)}
\usage{
er_ablation_table(
  data,
  truth = NULL,
  id_col = NULL,
  block_spec = "auto",
  cluster_methods = c("hclust_avg", "hclust_ward", "gc", "leiden", "louvain",
    "label_prop", "threshold_cc"),
  merge = "best",
  conditions = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{data.frame of records.}

\item{truth}{Optional ground truth (required for \code{full_ari} condition).}

\item{id_col}{Character. ID column.}

\item{block_spec}{Blocking specification for the \code{blocking} conditions.}

\item{cluster_methods}{Character vector.}

\item{merge}{Character. Post-processing strategy.}

\item{conditions}{Named list of conditions to run.  Default: all 6.}

\item{verbose}{Logical.}
}
\value{
data.frame with one row per (condition × method) combination and
columns \code{condition, weight_method, blocking, method, ARI, Bcubed_F,
  Vmeasure, Homogeneity, Completeness}.
}
\description{
Evaluates the ERBOT pipeline under all combinations of weight strategy
(\code{uniform}, \code{fs_em}, \code{full_ari}) and blocking
(\code{no_block}, \code{block}).  \strong{Note}: the \code{full_ari}
condition uses truth for both weight learning and evaluation and is
therefore an optimistic upper bound — it is included for reference only.
}
\details{
The CV conditions (\code{w=CV-learned}) are handled by \code{\link{er_cv}}.
}
