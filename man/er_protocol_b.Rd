% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-cv_protocols.R
\name{er_protocol_b}
\alias{er_protocol_b}
\title{Protocol B: CV5 tune -> consensus refit (honest estimate).}
\usage{
er_protocol_b(
  sim_list,
  pairs,
  n,
  truth_tbl,
  id_vec,
  method_grid = DEFAULT_METHOD_GRID,
  tau_grid = DEFAULT_TAU_GRID,
  weight_method = "ari",
  n_folds = 5L,
  base_seed = 42L,
  verbose = TRUE
)
}
\arguments{
\item{sim_list}{Named list of per-field similarity vectors.}

\item{pairs}{tibble(idx1, idx2).}

\item{n}{Integer. Number of records.}

\item{truth_tbl}{tibble(id, cluster_id). Ground truth.}

\item{id_vec}{Character vector of record IDs (length n).}

\item{method_grid}{Character vector of methods to sweep.}

\item{tau_grid}{Numeric vector of tau / merge_alpha values.}

\item{weight_method}{Character. Weight learning method. Default "ari".}

\item{n_folds}{Integer. Number of CV folds. Default 5L.}

\item{base_seed}{Integer. Base RNG seed; fold k uses base_seed + k. Default 42L.}

\item{verbose}{Logical. Print progress. Default TRUE.}
}
\value{
A list:
\describe{
\item{fold_best}{data.frame with columns fold, method, tau, train_ARI.}
\item{consensus_method}{Character. Consensus method.}
\item{consensus_tau}{Numeric. Consensus tau (nearest grid value to median).}
\item{ARI}{Numeric. Full-data ARI at consensus params.}
\item{Bcubed_F}{Numeric.}
\item{Vmeasure}{Numeric.}
\item{labels}{Integer vector of full-data cluster labels.}
}
}
\description{
For each of 5 folds: sweep (method, tau) grid on the training 80\%, record
the best params by training ARI. Select consensus params (tau = median,
method = majority vote) across folds. Refit on full data with consensus
params. Report the single full-data ARI as the honest performance estimate.
}
