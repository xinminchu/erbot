% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/50-tuning.R
\name{er_eval_metrics}
\alias{er_eval_metrics}
\title{ER quality metrics: CPM, modularity, silhouette, DB, CH, stability, pairwise & B3}
\usage{
er_eval_metrics(
  g = NULL,
  membership,
  X = NULL,
  D = NULL,
  gamma = 0.05,
  weights = NULL,
  stability_labels = NULL,
  truth = NULL,
  dist_method = "euclidean"
)
}
\arguments{
\item{g}{igraph graph (optional; for CPM/modularity).}

\item{membership}{Integer vector of cluster labels (length = n).}

\item{X}{Numeric matrix/data.frame of features/embeddings (rows = n) (optional; for silhouette/DB/CH).}

\item{D}{Optional distance object/matrix for silhouette (if provided, X is ignored for silhouette).}

\item{gamma}{Numeric CPM resolution parameter (default 0.05).}

\item{weights}{Optional edge weights vector matching E(g) (defaults to E(g)$weight if present).}

\item{stability_labels}{Optional list of label vectors (same length as membership) for stability ARI.}

\item{truth}{Optional integer/factor vector of gold cluster ids (length = n) for external metrics.}

\item{dist_method}{Distance method for silhouette if D is not given (default "euclidean").}
}
\value{
Named list with available metrics.
}
\description{
Unified helpers to score graph/vectors clusterings in ER.
\itemize{
\item Graph objectives: CPM (resolution gamma), modularity.
\item Vector metrics: Silhouette, Davies–Bouldin (DB), Calinski–Harabasz (CH).
\item Stability: mean pairwise ARI across multiple runs (labels of same length).
\item External with truth: Pairwise P/R/F1 (no explicit pair enumeration), B³ P/R/F1, ARI.
}
}
