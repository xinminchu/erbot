% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-cv_protocols.R
\name{.subset_to_fold}
\alias{.subset_to_fold}
\title{Subset sim_list, pairs, id_vec, and truth_vec to a set of record indices.}
\usage{
.subset_to_fold(sim_list, pairs, id_vec, truth_vec, idx)
}
\arguments{
\item{sim_list}{Named list of numeric vectors (length = nrow(pairs)).}

\item{pairs}{tibble(idx1, idx2) from er_block().}

\item{id_vec}{Character vector of record IDs (length n).}

\item{truth_vec}{Integer vector of ground-truth labels (length n), or NULL.}

\item{idx}{Integer vector of record indices to keep (subset of 1..n).}
}
\value{
list(sim_list, pairs, n, id_vec, truth_vec) with remapped indices.
}
\description{
Filters pairs to those where both records are in idx, then remaps
idx1/idx2 from global 1..n to local 1..length(idx).
}
\keyword{internal}
