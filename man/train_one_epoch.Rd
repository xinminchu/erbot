% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/45-training_loop.R
\name{train_one_epoch}
\alias{train_one_epoch}
\title{Train one epoch of the stable graph-neural ER model}
\usage{
train_one_epoch(
  encoder,
  optimizer,
  X,
  pos_pairs,
  neg_pairs,
  lambda_graph = 0.001,
  lambda_K = 0.001,
  k_nn = 10,
  perturb_cfg = list(noise_sd = 0.01, p_dropout = 0.05),
  clustering_method = "leiden"
)
}
\arguments{
\item{encoder}{A RecordEncoder nn_module.}

\item{optimizer}{A torch optimizer.}

\item{X}{Torch tensor \code{c([n, p])} of input features.}

\item{pos_pairs}{Data frame of positive pairs (i, j).}

\item{neg_pairs}{Data frame of negative pairs (i, j).}

\item{lambda_graph}{Weight of the graph smoothness term.}

\item{lambda_K}{Weight of the stability term.}

\item{k_nn}{Number of neighbours in kNN graph.}

\item{perturb_cfg}{List with entries noise_sd, p_dropout.}

\item{clustering_method}{"leiden" or "louvain".}
}
\value{
A list with loss components.
}
\description{
This routine:
\enumerate{
\item computes embeddings via the encoder,
\item builds a kNN graph and a perturbed graph,
\item runs graph clustering on both,
\item constructs projection matrices,
\item computes contrastive, graph smoothness, and stability losses,
\item backpropagates into the encoder via the contrastive loss
(graph and stability losses are treated as external regularizers here).
}
}
