% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er_run.R, R/run_datasets.R
\name{er_run}
\alias{er_run}
\alias{er_main}
\alias{er_main_simple}
\alias{er_unified_pipeline}
\alias{er_general_pipeline}
\title{Run the full ERBOT entity resolution pipeline}
\usage{
er_run(
  data,
  truth = NULL,
  id_col = NULL,
  text_cols = NULL,
  mode = "auto",
  block = "auto",
  similarity = "auto",
  weights = "auto",
  cluster_methods = "all",
  k = NULL,
  k_grid = c(5L, 10L, 15L, 20L, 30L, 50L),
  threshold = 0.5,
  merge = "best",
  consensus_alpha = 0.5,
  eval_mode = "labeled_only",
  out_dir = NULL,
  verbose = TRUE
)

er_main(
  data,
  truth = NULL,
  id_col = NULL,
  text_cols = NULL,
  mode = "auto",
  block = "auto",
  similarity = "auto",
  weights = "auto",
  cluster_methods = "all",
  k = NULL,
  k_grid = c(5L, 10L, 15L, 20L, 30L, 50L),
  threshold = 0.5,
  merge = "best",
  consensus_alpha = 0.5,
  eval_mode = "labeled_only",
  out_dir = NULL,
  verbose = TRUE
)

er_main_simple(
  data,
  truth = NULL,
  id_col = NULL,
  text_cols = NULL,
  mode = "auto",
  block = "auto",
  similarity = "auto",
  weights = "auto",
  cluster_methods = "all",
  k = NULL,
  k_grid = c(5L, 10L, 15L, 20L, 30L, 50L),
  threshold = 0.5,
  merge = "best",
  consensus_alpha = 0.5,
  eval_mode = "labeled_only",
  out_dir = NULL,
  verbose = TRUE
)

er_unified_pipeline(
  data,
  truth = NULL,
  id_col = NULL,
  text_cols = NULL,
  mode = "auto",
  block = "auto",
  similarity = "auto",
  weights = "auto",
  cluster_methods = "all",
  k = NULL,
  k_grid = c(5L, 10L, 15L, 20L, 30L, 50L),
  threshold = 0.5,
  merge = "best",
  consensus_alpha = 0.5,
  eval_mode = "labeled_only",
  out_dir = NULL,
  verbose = TRUE
)

er_general_pipeline(
  data,
  truth = NULL,
  id_col = NULL,
  text_cols = NULL,
  mode = "auto",
  block = "auto",
  similarity = "auto",
  weights = "auto",
  cluster_methods = "all",
  k = NULL,
  k_grid = c(5L, 10L, 15L, 20L, 30L, 50L),
  threshold = 0.5,
  merge = "best",
  consensus_alpha = 0.5,
  eval_mode = "labeled_only",
  out_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame}, file path, or benchmark keyword
(\code{"cora"}, \code{"affiliation"}, \code{"d10k"}).}

\item{truth}{Optional ground truth: named integer vector, data.frame
(id, cluster_id), or file path.  If provided, supervised metrics are
computed and supervised weight learning is used.}

\item{id_col}{Character. ID column name.  Auto-detected if \code{NULL}.}

\item{text_cols}{Character vector.  Columns to use.  Auto-detected from
\code{er_diagnose()} if \code{NULL}.}

\item{mode}{Character. \code{"auto"}, \code{"dedup"}, or \code{"link"}.
Auto-detection checks for a \code{source_id} column.}

\item{block}{Character or list. Blocking method: \code{"auto"},
\code{"none"}, \code{"standard"}, \code{"prefix"}, \code{"sn"}.
Or a named list \code{list(method=..., key=..., prefix_len=...,
  sn_window=...)}.}

\item{similarity}{List of field specs, or \code{"auto"}.  If
\code{"auto"}, spec is built from \code{er_diagnose()}.}

\item{weights}{Field weight specification.  One of \code{"auto"},
\code{"equal"}, \code{"ari"}, \code{"fellegi_sunter"}, \code{"bimodal"},
\code{"variance"}, or a named numeric vector.}

\item{cluster_methods}{Character vector of clustering methods, or
\code{"all"}.  See \code{er_cluster_all()}.}

\item{k}{Integer. Cluster count for centroidal methods.  Tuned
automatically if \code{NULL}.}

\item{k_grid}{Integer vector. k values swept during auto-tuning.}

\item{threshold}{Numeric. Similarity threshold for \code{threshold_cc} and
graph coloring.}

\item{merge}{Character. Post-processing: \code{"transitivity"},
\code{"consensus"}, \code{"best"}, or \code{"none"}.}

\item{consensus_alpha}{Numeric. Fraction for consensus merge.}

\item{eval_mode}{Character. \code{"labeled_only"} or
\code{"singleton_fill"}.}

\item{out_dir}{Character. If set, write CSV and PDF report here.}

\item{verbose}{Logical. Print stage-level messages.}
}
\value{
A named list (class \code{"er_result"}) with elements:
\describe{
\item{\code{clusters}}{Integer vector of final cluster labels (length n).}
\item{\code{all_clusters}}{Named list of label vectors per method.}
\item{\code{performance}}{tibble of evaluation metrics (NULL if no truth).}
\item{\code{unsupervised}}{Named list of unsupervised quality scores.}
\item{\code{predictions}}{tibble(id, cluster_id) for CSV output.}
\item{\code{diag}}{Output of \code{er_diagnose()}.}
\item{\code{blocking}}{List: pairs tibble and stats.}
\item{\code{weights}}{Named numeric weight vector used.}
\item{\code{params}}{List of all parameters used.}
}
}
\description{
The single-call interface to \pkg{erbot}.  Given any tabular dataset,
identifies which records refer to the same real-world entity.
}
