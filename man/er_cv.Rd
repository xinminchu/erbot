% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11-ablation.R
\name{er_cv}
\alias{er_cv}
\title{Generic K-fold cross-validation for the ERBOT pipeline}
\usage{
er_cv(
  data,
  truth,
  id_col = NULL,
  K = 5L,
  seed = 42L,
  block = "auto",
  weights = "ari",
  cluster_methods = c("hclust_avg", "hclust_ward", "gc", "leiden", "louvain",
    "label_prop", "threshold_cc"),
  merge = "best",
  out_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{data.frame of records.}

\item{truth}{Ground truth (required for ARI weight learning and evaluation).}

\item{id_col}{Character. ID column. Auto-detected if NULL.}

\item{K}{Integer. Number of folds (default 5).}

\item{seed}{Integer. Random seed for fold assignment (default 42).}

\item{block}{Blocking specification: \code{"none"}, \code{"auto"}, a
keyword string, or a named list with \code{method}, \code{key},
\code{prefix_len}, etc.}

\item{weights}{Character. Weight learning method for the train fold.
One of \code{"ari"} (default), \code{"equal"}, \code{"fellegi_sunter"}.}

\item{cluster_methods}{Character vector of clustering methods.}

\item{merge}{Character. Post-processing merge strategy (default \code{"best"}).}

\item{out_dir}{Character. If set, write per-fold CSVs here.}

\item{verbose}{Logical.}
}
\value{
A named list:
\item{\code{cv_summary}}{data.frame: mean+SD per method across test folds.}
\item{\code{all_folds}}{data.frame: per-fold per-method test metrics.}
\item{\code{fold_weights}}{List of learned weight vectors per fold.}
\item{\code{n_folds_ok}}{Integer: number of folds that completed.}
}
\description{
Partitions records into K random folds, learns field weights on each
80\\% train fold, and evaluates on the held-out 20\\% test fold.
This is the honest out-of-sample evaluation protocol used in the paper.
}
\details{
Weight learning uses \code{method = weights} on the training fold only;
the resulting \code{learned_weights} vector is passed directly to
\code{er_run()} for the test fold (no further fitting on test data).
}
